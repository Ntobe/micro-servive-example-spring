server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: github.com:Ntobe/micro-services-demo-config-server.git
            clone-on-start: true
